import { MenuItem } from "../../util/menu-item.interface";

/**
 * The markup interface for all items that represent a dynamic route in the
 * application.
 */
export interface RouteItem extends MenuItem {}
